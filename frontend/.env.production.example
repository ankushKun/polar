# Worker co-deploy (default `npm run deploy`) - same-origin API; preview URLs from API viewUrl
VITE_API_BASE=/api

# Walrus frontend (polar.wal.app) - build manually with:
# VITE_API_BASE=https://glacier.construct-computer.workers.dev/api
# VITE_PORTAL_SUBDOMAIN_BASE=polar.ankush.one
