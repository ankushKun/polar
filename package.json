{
  "name": "glacier",
  "private": true,
  "scripts": {
    "setup:env": "node scripts/setup-env.mjs",
    "install:all": "npm install --prefix worker && npm install --prefix frontend && npm install --prefix preview-worker",
    "dev": "concurrently -n worker,frontend -c blue,magenta \"npm run dev --prefix worker\" \"npm run dev --prefix frontend\"",
    "dev:worker": "npm run dev --prefix worker",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:preview": "npm run dev --prefix preview-worker",
    "build:frontend": "npm run build:frontend --prefix worker",
    "deploy": "npm run deploy --prefix worker",
    "deploy:preview": "npm run deploy --prefix preview-worker"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
